# MemTest v4 dependencies
# Python >= 3.10 required

# Core (zero external dependencies for basic usage)
# - extractor.py needs an LLM API for best results (DeepSeek/OpenAI compatible)
# - runner.py, quality_check.py, schema.py are pure Python stdlib

# Optional: for TF-IDF / embedding baselines
# numpy
# scikit-learn
# jieba

# Optional: for LLM extraction (choose one)
# openai>=1.0
# Or use the built-in urllib adapter with DEEPSEEK_API_KEY
