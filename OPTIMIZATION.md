# MemTest 生成器问题诊断与优化方向

> **区分**：
> - **generator.py** = 程序化生成（自己造数据，零依赖）
> - **knowledge_builder.py** = 语料输入生成（从文本提取，需要 LLM）

---

## 一、程序化生成（generator.py）的问题

| 问题 | 严重程度 | 状态 | 说明 |
|------|---------|------|------|
| **数据池太小** | 🔴 高 | ✅ 已修复 | 城市10→100，人名8→200+，地点7→70+，产品7→100+ |
| **版本文本过短** | 🔴 高 | ✅ 已修复 | 语义差异化：客观/主观/转述三种视角 |
| **重复查询** | 🟡 中 | 🟡 部分 | 模板化问题，需改用LLM或更丰富的模板池 |
| **聚类单一** | 🟡 中 | ✅ 已修复 | 现在4-6个cluster，每cluster 3-8条 |
| **推理链缺失** | 🟡 中 | ✅ 已修复 | 5种逻辑类型（因果/时序/对比/包含/推导），各3-6跳 |
| **无负样本查询** | 🔴 高 | ✅ 已修复 | 20%负样本（人物/地点/事件不存在，或组合矛盾） |
| **时间戳精度** | 🟢 低 | 🟢 不变 | 仍是当前时间回溯，但范围扩展到2年 |
| **事件类型单一** | 🟢 低 | ✅ 已修复 | 从3类扩展到20类（情感/冲突/发现/技术/金融等） |
| **versions 3种差异不够** | 🟢 低 | ✅ 已修复 | 客观叙述/主观视角/第三方转述，真正语义差异 |

---

## 二、语料输入生成（knowledge_builder.py）的问题

| 问题 | 严重程度 | 状态 | 说明 |
|------|---------|------|------|
| **LLM 事实提取格式不稳定** | 🔴 高 | 🟡 部分 | 已用 `generate_json()`，但提示词仍缺 few-shot（已补） |
| **版本生成质量差** | 🔴 高 | 🟡 部分 | 提示词已加 few-shot，但 generator 程序化版本仍有模板感 |
| **查询生成依赖程序化模板** | 🟡 中 | 🟡 待做 | 未接入 LLM 查询生成（知识构建器） |
| **缺少跨语言支持** | 🟡 中 | 🟢 不变 | 英文文本处理仍不健壮 |
| **链构建过于简单** | 🟡 中 | 🟡 待做 | 提示词已加 chain_id 提取，但代码未利用 |
| **无负样本查询** | 🟡 中 | 🟡 待做 | 知识构建器未生成负样本 |
| **字段标准化粗糙** | 🟢 低 | 🟢 不变 | location 简单 split |
| **无质量校验** | 🟡 中 | ✅ 已修复 | 新增 `quality_check.py`，10 项自动检查 |

---

## 三、提示词（prompts/）的问题

| 问题 | 严重程度 | 状态 | 说明 |
|------|---------|------|------|
| **prompts 没有 few-shot 示例** | 🟡 中 | ✅ 已修复 | 3个提示词全部加了 few-shot（memory_enhance: 2例，query_generate: 2例，fact_extract: 3例） |
| **query_generate 提示词缺少"难度"判断标准** | 🟡 中 | ✅ 已修复 | 新增难度定义表（简单/中等/困难）+ 类型矩阵 |
| **无 chain 生成提示词** | 🟡 中 | 🟡 待做 | 代码已有链生成，但缺独立 `chain_extract.md` 提示词 |
| **无负样本生成提示词** | 🟡 中 | 🟡 待做 | 知识构建器未接入负样本生成 |
| **fact_extract 的 dynasty 字段在英文文本中无用** | 🟢 低 | 🟢 不变 | 对 HP/英文小说 benchmark 不适用 |

---

## 四、程序化生成优化方向（generator.py）

### 方向1：扩大数据池 + 提升多样性

```python
# 当前：CITIES = 10个，应扩展到 50-100 个
# 当前：人名8个，应扩展到 200+ 个（含英文）
# 新增事件类型：情感、冲突、发现、转折、意外、灾难等

# 建议：按"领域"分数据池
DOMAINS = {
    "business": {"cities": [...], "people": [...], "events": [...]},
    "historical": {"dynasties": [...], "figures": [...], "events": [...]},
    "scifi": {"planets": [...], "characters": [...], "events": [...]},
    "daily": {"cities": [...], "people": [...], "events": [...]},
}
```

### 方向2：真正的语义多样化版本

当前：3 个版本只是模板拼接，长度/用词差异不大。

优化：让 versions 之间有**真正的语义差异**：
- v1 标准：客观描述，含时间地点人物
- v2 主观：从人物视角，含心理活动/评价
- v3 间接：第三方转述，含推测/省略细节

### 方向3：负样本查询（关键缺失）

当前：所有查询都有预期答案，无法测试 false positive。

优化：生成两类负样本：
1. **相关但无关**：人物正确，但事件/地点错误（如"张伟在星巴克购买茅台"→实际在肯德基）
2. **完全不相关**：人物/事件/地点都不在数据库中

### 方向4：链式数据生成（推理测试核心）

当前：reasoning_chain=0，无链式数据。

优化：显式构建因果链：
```json
{
  "memory_id": "MEM001",
  "reasoning_chain": "CHAIN_001",
  "chain_position": 1,
  "cause": "",
  "effect": "MEM002"
}
```

需要新增 `chain_generate` 提示词，让 LLM 从文本中提取因果序列。

### 方向5：聚类数据生成（整理测试）

当前：只生成1个cluster。

优化：显式构建多主题聚类：
- 同一人物的多个事件 → person_cluster
- 同一地点的多个事件 → location_cluster
- 同一产品/项目的多个事件 → topic_cluster
- 同一时间段的多个事件 → temporal_cluster

每个 cluster 3-8 条记忆，确保有合理的聚类测试数据。

---

## 五、语料输入生成优化方向（knowledge_builder.py）

### 方向6：LLM 提取稳定性

- 改用 `generate_json()` 方法（已提供），自动处理 markdown 包裹
- 批量提取：每篇文章一个 LLM call → 改为每 BATCH 篇文章一个 call
- 失败重试：3 次失败回退到简单提取模式

### 方向7：版本生成质量

- 用 LLM 生成真正的 3 种表达风格（而非模板拼接）
- 或至少让模板拼接更自然：加入转折词、省略次要信息

### 方向8：查询生成质量

- 用 LLM 从事实生成多样化查询（而非模板化）
- 生成负样本查询（与某记忆相关但不对）

### 方向9：链构建改进

- 不只看人物，看**因果连接词**："因为...所以..."、"导致..."、"随后..."
- 用 LLM 判断事件间是否有因果关系
- 链长度 3-6 跳，覆盖简单/中等/困难

### 方向10：性能优化

```python
# 阶段2：每个字段一个 call → 改为批量分类（30个词/次）
# 阶段6：每个查询一个 call → 改为批量预解析（15个/次）
# 利用 llm_interface.py 的 batch_generate() 并行
```

---

## 六、提示词优化方向（prompts/）

| 提示词 | 优化 |
|--------|------|
| `memory_enhance` | 加入 few-shot 示例，明确输出格式，增加"主观视角"和"间接转述"版本 |
| `query_generate` | 加入负样本生成指令，明确难度判断标准，加 few-shot |
| `fact_extract` | 支持中英文，加入 "chain_id" 提取，让 LLM 标注事件间的因果关系 |
| 新增 `chain_extract` | 从文本提取事件序列，标注 cause→effect 关系 |
| 新增 `negative_query` | 生成与某记忆"相关但不对"的查询，用于测试 precision |

---

## 七、通用优化：数据质量校验

新增 `quality_check.py`（生成后运行）：
- 检查所有 expected_memory_ids 是否有效
- 检查查询是否有重复文本
- 检查版本文本长度分布是否合理
- 检查类别/难度分布是否均衡
- 输出质量报告，不达标可自动重试

---

## 八、优先级建议

### 程序化生成（P0-P2）

| 优先级 | 任务 | 预期收益 |
|--------|------|---------|
| P0 | 负样本查询生成 | 可测 precision，当前无法测 |
| P0 | 扩大数据池（200+人名/50+城市） | 减少重复，提升 realism |
| P1 | 链式数据生成（chain_extract） | 推理测试有数据可测 |
| P1 | 多聚类生成（4-6个cluster） | 整理测试有数据可测 |
| P2 | 版本语义差异化 | 检索 recall 更真实 |
| P2 | 质量校验脚本 | 自动发现问题 |

### 语料输入生成（P1-P3）

| 优先级 | 任务 | 预期收益 |
|--------|------|---------|
| P1 | 提示词加 few-shot | 提升LLM输出稳定性 |
| P1 | 批量LLM调用优化 | 生成速度提升3-5x |
| P2 | 链构建改进（因果关系） | 推理链更真实 |
| P2 | 负样本查询 | 可测 precision |
| P3 | 跨语言支持完善 | 英文 benchmark 更自然 |
| P3 | 按领域分数据池 | 支持更多 benchmark 场景 |

---

## 十、已完成改动（2026-05-31）

### 程序化生成（generator.py）

| 改动 | 状态 | 验证 |
|------|------|------|
| 数据池扩到100+城市/200+人名/70+地点/100+产品 | ✅ | 100条记忆用68城市/73人名/65产品 |
| 加20%负样本查询 | ✅ | 50条查询中10条负样本 |
| 生成4-6个不同cluster | ✅ | 5个cluster，各3-4条记忆 |
| 链式数据生成（5种逻辑类型） | ✅ | 5条链，25条记忆，3-6跳 |
| 版本语义差异化（3种视角） | ✅ | 客观/主观/转述 |
| 质量校验脚本 | ✅ | 10项检查，1警告，0错误 |
| 提示词加 few-shot | ✅ | 3个模板都加了示例 |
| 对比链修复 | ✅ | 真实动词替代伪动词 |

### 语料输入生成（knowledge_builder.py）

| 改动 | 状态 | 说明 |
|------|------|------|
| 提示词加 few-shot | ✅ | fact_extract 已加3个示例 |
| 质量校验 | ✅ | 可用 quality_check.py 检查输出 |
| 链式检测提示词 | ✅ | 已标注 chain_id 提取规则 |

---

## 十一、下一步建议

### 知识构建器（剩余工作）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 负样本查询生成 | P1 | ✅ 已修复 | generator.py 20%负样本 + knowledge_builder.py 20%负样本 |
| 链式数据利用 | P1 | ✅ 已修复 | 利用 LLM 提取的 chain_id 构建链，fallback 到人物分组 |
| 批量LLM调用 | P2 | 知识构建器仍逐条调用，速度可优化3-5x |
| 跨语言支持 | P3 | 英文文本处理健壮性 |

### 通用（可选增强）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 按领域分数据池 | P2 | 支持更多 benchmark 场景（历史/科幻/日常） |
| 新增 chain_extract.md 提示词 | P2 | 独立提示词文件，从文本提取事件序列 |
| 新增 negative_query.md 提示词 | P2 | 独立提示词文件，生成负样本查询 |
| 查询模板池扩容 | P2 | 减少查询重复（当前仍有5-10%重复） |